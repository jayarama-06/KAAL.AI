
  {
    "name": "kaal-ai",
    "version": "1.0.0",
    "private": true,
    "workspaces": [
      "src"
    ],
    "engines": {
      "node": ">=18.18.0"
    },
    "scripts": {
      "dev": "npm run dev --workspace src",
      "build": "npm run build --workspace src",
      "lint": "npm run lint --workspace src",
      "typecheck": "npm run typecheck --workspace src",
      "test": "npm run test --workspace src",
      "test:coverage": "npm run test:coverage --workspace src"
    }
  }